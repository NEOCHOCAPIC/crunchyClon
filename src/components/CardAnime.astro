---
// CardAnime.astro
const {
  imagen,
  titulo,
  descripcion,
  rating,
  votos,
  temporadas,
  episodios,
  doblaje,
} = Astro.props;
---

<div
  class="relative group w-[80%] sm:w-1/2 md:w-1/3 lg:w-1/4 xl:w-1/5 flex-shrink-0 px-2 mx-auto"
>
  <div class="browse-card relative h-full p-2">
    <a href="#" class="block relative">
      <div class="aspect-[4/5] overflow-hidden rounded-lg">
        <img
          src={imagen}
          alt={titulo}
          class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
          loading="lazy"
        />
      </div>
    </a>

    <div class="p-3">
      <h4 class="font-semibold text-white text-sm truncate sm:text-base">
        <a href="#" class="hover:text-blue-400">{titulo}</a>
      </h4>
      <div class="mt-1">
        <span class="text-gray-400 text-xs">{doblaje}</span>
      </div>
    </div>

    <!-- Hover card -->
    <div
      class="opacity-0 invisible group-hover:opacity-100 group-hover:visible absolute inset-0 bg-gray-900/95 rounded-lg p-4 transition-all duration-300 z-20"
    >
      <div class="flex flex-col h-full">
        <h4 class="font-semibold text-white text-sm mb-2">{titulo}</h4>

        <div class="flex items-center gap-2 mb-2">
          <div class="flex items-center text-yellow-400">
            <span class="text-sm font-semibold">{rating}</span>
            <svg class="w-4 h-4 ml-1 fill-current" viewBox="0 0 24 24">
              <path
                d="M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z"
              ></path>
            </svg>
          </div>
          <span class="text-gray-400 text-xs">{votos}</span>
        </div>

        <div class="text-gray-400 text-xs mb-2">
          <span class="mr-2">{temporadas}</span>
          <span>{episodios}</span>
        </div>

        <p class="text-gray-300 text-xs line-clamp-4 mb-4">
          {descripcion}
        </p>

        <div class="mt-auto flex gap-3">
          <button
            class="p-2 rounded-full bg-gray-800 hover:bg-gray-700 transition-colors"
          >
            <svg
              class="w-5 h-5 text-white"
              viewBox="0 0 24 24"
              fill="currentColor"
            >
              <path d="M8 5v14l11-7z"></path>
            </svg>
          </button>
          <button
            class="p-2 rounded-full bg-gray-800 hover:bg-gray-700 transition-colors"
          >
            <svg
              class="w-5 h-5 text-white"
              viewBox="0 0 24 24"
              fill="currentColor"
            >
              <path d="M19 13h-6v6h-2v-6H5v-2h6V5h2v6h6v2z"></path>
            </svg>
          </button>
        </div>
      </div>
    </div>
  </div>
</div>
